# ── Frontend (Vite) ────────────────────────────────────────────────────────
# Base URL of the backend API.
# In development, leave empty to use the Vite proxy: /api → http://localhost:5000
# In production, set this to your deployed backend, e.g. https://api.example.com/api
VITE_API_URL=
